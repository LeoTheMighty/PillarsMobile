import React from 'react';
import { View } from 'react-native';

/**
 * This will house the Settings view which will probably come up as a modal from the main screen after a button. Should
 * control all the specific things about the app functioning.
 *
 * @constructor
 */
const SettingsView = () => {
  return <View style={styles.main} />;
};

const styles = StyleSheet.create({
  main: {},
});

export default SettingsView;
